# Telegram Weather Bot

A simple Telegram bot that provides **current weather**, **daily**, and **weekly forecasts** for selected cities.

The bot interacts with users through a step-by-step interface (**city → time interval**) and returns formatted weather data directly in chat.

Weather data is fetched using the **Open-Meteo API** ([open-meteo.com](https://open-meteo.com/))